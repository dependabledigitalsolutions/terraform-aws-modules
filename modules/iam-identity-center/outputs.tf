# No outputs declared in v0.1.0 — account assignments are made inside the
# upstream module call so consumers rarely need to reference its returns.
# If you need an upstream attribute, add a passthrough here and tag a release.
